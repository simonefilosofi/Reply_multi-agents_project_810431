# Pinned to the versions the recorded runs under runs/ were produced with, on Python 3.13.
# Exact pins on the direct dependencies only: a full environment freeze would carry
# Windows-only packages and break an install on macOS or Linux.

pandas==2.3.3
numpy==2.4.4
matplotlib==3.10.9
pydantic==2.13.3
langgraph==0.6.11
langchain-core==1.6.1
langchain-deepseek==1.1.0
openai==2.32.0
streamlit==1.56.0
markdown==3.10.3
e2b-code-interpreter==2.6.2
python-dotenv==1.2.2
pytest==9.0.3

# The notebook front-end the README's `jupyter notebook main.ipynb` needs.
# nbconvert and ipykernel arrive with it and are what execute the notebook non-interactively.
notebook==7.6.2
